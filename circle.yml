machine:
  services:
    - redis
  hosts:
    redis: 127.0.0.1
  ruby:
    version: 2.1.2
  node:
    version: iojs-v1.8
dependencies:
  pre:
    - echo //registry.npmjs.org/:_authToken=${NPM_TOKEN} > .npmrc
general:
  artifacts:
    - coverage/*
    - coverage/**/*
    - log/*.log
    - npm-debug.log
